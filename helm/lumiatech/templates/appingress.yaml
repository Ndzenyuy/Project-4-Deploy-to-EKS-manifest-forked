apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lumia-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lumia-app-service
            port:
              number: 8080
